import { StatusBar } from "expo-status-bar";import { useCallback, useEffect, useState } from "react";import { ActivityIndicator, Linking, SafeAreaView, StyleSheet, Text, View } from "react-native";import { WebView, type WebViewNavigation } from "react-native-webview";import type { WebViewProgressEvent } from "react-native-webview/lib/WebViewTypes";const WEB_APP_URL = process.env.EXPO_PUBLIC_WEB_APP_URL ?? "https://bloomplan-sfqewszz.manus.space";const LOAD_TIMEOUT_MS = 12_000;export default function App() {  const [loading, setLoading] = useState(true);  const [failed, setFailed] = useState(false);  const [webViewKey, setWebViewKey] = useState(0);  useEffect(() => {    const timeout = setTimeout(() => {      // Android WebView can keep a document in a loading state while a      // long-lived resource is pending. The page is still usable, so do not      // hide it behind the native loading overlay indefinitely.      setLoading(false);    }, LOAD_TIMEOUT_MS);    return () => clearTimeout(timeout);  }, [webViewKey]);  const reload = useCallback(() => {    setFailed(false);    setLoading(true);    setWebViewKey(value => value + 1);  }, []);  const handleNavigation = useCallback((request: WebViewNavigation) => {    const target = request.url;    if (target.startsWith("mailto:") || target.startsWith("tel:") || target.startsWith("intent:")) {      void Linking.openURL(target);      return false;    }    return true;  }, []);  const handleProgress = useCallback((event: WebViewProgressEvent) => {    if (event.nativeEvent.progress >= 0.6) setLoading(false);  }, []);  return (    <SafeAreaView style={styles.safe}>      <StatusBar style="dark" />      <View style={styles.container}>        <WebView          key={webViewKey}          source={{ uri: WEB_APP_URL }}          originWhitelist={["*"]}          onLoadStart={() => { setLoading(true); setFailed(false); }}          onLoadProgress={handleProgress}          onLoadEnd={() => setLoading(false)}          onError={() => { setLoading(false); setFailed(true); }}          onHttpError={() => { setLoading(false); setFailed(true); }}          onShouldStartLoadWithRequest={handleNavigation}          sharedCookiesEnabled          thirdPartyCookiesEnabled          javaScriptEnabled          domStorageEnabled          cacheEnabled          allowsBackForwardNavigationGestures          setSupportMultipleWindows={false}        />        {loading && <View pointerEvents="none" style={styles.overlay}><Loading /></View>}        {failed && <View style={styles.errorCard}>          <Text style={styles.errorTitle}>اتصال به Bloom Planner برقرار نشد</Text>
